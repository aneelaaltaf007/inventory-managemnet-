## ⚙️ Commands

### 📥 Clone the Repository

```bash
git clone YOUR_REPOSITORY_URL
```

### 📁 Go to the Project

```bash
cd inventory-management/inventory-managemnet-
```

### 📂 Go to the Streamlit Application

```bash
cd streamlit/01_app_test
```

### 🐍 Activate Conda Environment

```bash
conda activate streamlit_env
```

### 🔍 Check Python Version

```bash
python --version
```

### 📦 Install Streamlit

```bash
python -m pip install streamlit
```

### 🔎 Check Streamlit Version

```bash
python -m streamlit --version
```

### ▶️ Run the Streamlit Application

```bash
python -m streamlit run app.py
```

The application will normally be available at:

```text
http://localhost:8501
```

---

## 🔧 Git Commands

### Check Repository Status

```bash
git status
```

### Check Current Branch

```bash
git branch
```

### Add Changes

```bash
git add .
```

### Commit Changes

```bash
git commit -m "Update project"
```

### Push Changes to GitHub

```bash
git push origin main
```

### Pull Latest Changes

```bash
git pull origin main
```

### Check Remote Repository

```bash
git remote -v
```

---

## 🔄 Complete Workflow

After making changes to the project:

```bash
cd ~/inventory-management/inventory-managemnet-/streamlit/01_app_test
```

```bash
conda activate streamlit_env
```

```bash
python -m streamlit run app.py
```

After editing the project:

```bash
git status
git add .
git commit -m "Update Streamlit app"
git push origin main
```

## 🛑 Stop Streamlit

To stop the running Streamlit server, press:

```text
Ctrl + C
```

